version: 1.2.8
cache: true
endpoints:
  custom:
    - name: "Claude"
      apiKey: "anything"  # Can be any string
      baseURL: "http://localhost:8000/v1"
      models:
        default: ["claude-3-5-sonnet"]
      fetch: false
      modelDisplayLabel: "Claude"
    
    - name: "Claude Subscription (via Proxy)"
      apiKey: "dummy-key"  # Can be any string since we're using your subscription
      baseURL: "http://localhost:4000/v1"
      models:
        default:
          - "claude-3-5-sonnet-20241022"
          - "claude-3-5-haiku-20241022"
          - "claude-3-opus-20240229"
        fetch: false
      modelDisplayLabel: "Claude (Unlimited)"
      titleModel: "claude-3-5-haiku-20241022"
      summarize: false
      dropParams: []
      skipCompletion: false
      directEndpoint: false